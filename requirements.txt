# Base de datos
mysql-connector-python==9.1.0
python-dotenv==1.0.1
certifi==2024.8.30

# Visión por computadora y OCR
opencv-python==4.10.0.84
pytesseract==0.3.13
Pillow==11.0.0

# Dashboard web (Flask -- versión original, sin autenticación; se mantiene
# en paralelo hasta confirmar el reemplazo definitivo por el panel Django)
Flask==3.1.0

# Dashboard web (Django) -- PyMySQL en vez de mysqlclient para no
# depender de un compilador C en Windows
Django==5.1.15
PyMySQL==1.1.3
